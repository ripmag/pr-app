import Types from "./classes/types";

const AppConfig = {
    languages: [
        Types.LANGUAGE_EN,
        Types.LANGUAGE_RU,
    ],
    colorsThemes: [
        Types.DARK,
        Types.LIGHT,
    ],
}

export default AppConfig;